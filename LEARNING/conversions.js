const myAge = `16`

if (Number(myAge)>=18) {
    console.log(`Yes you are eligible to get a license at the age of ${myAge}`)
} else {
    console.log(`No you are not eligible to get a license at the age of ${myAge}`)
}